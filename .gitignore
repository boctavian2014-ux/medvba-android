# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/
# Bun is the only lockfile for this repo (EAS + expo-doctor). Do not commit npm's lockfile.
package-lock.json

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native (iOS generated locally / CI; Android is versioned for bare workflow)
ios/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files and secrets (never commit)
.env
.env*.local
.env.production
.env.*.local

# typescript
*.tsbuildinfo
.vercel

.rork

rork-eslint.config.js